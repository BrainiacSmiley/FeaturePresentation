<h1><%= @feature_description.name %></h1>
<h2><%= "#{t(:title_feature_edit)} | #{@feature.name}" %> </h2>
<%= form_for(@feature) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t(:button_feature_update) %>
  </div>
<% end %>
